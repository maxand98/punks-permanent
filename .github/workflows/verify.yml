name: Verify permanent release

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run release
      - uses: actions/upload-artifact@v4
        with:
          name: punks-permanent-release
          path: |
            dist/release-manifest.json
            dist/route-manifest.json
            dist/SHA256SUMS
          if-no-files-found: error
          retention-days: 30
